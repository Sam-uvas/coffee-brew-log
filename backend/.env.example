# Copy this file to .env and adjust as needed.

# SQLite file used by Sequelize. Path is relative to the backend folder.
DATABASE_URL="file:./dev.db"

# Port the Express server listens on.
PORT=4000

# Origin allowed to call the API (your frontend dev/prod URL).
CORS_ORIGIN="http://localhost:5173"
